#include "programvars.h"

int instructionHandle(char *, char *, int*, symbolRow *, instructionRow *);

void freeInstTable(instructioRow *);

int instHandleSecPass(char *, int*, symbolRow *, instructioRow *, externRow *);

